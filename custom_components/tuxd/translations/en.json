{
  "config": {
    "step": {
      "user": {
        "title": "Set up TuxD",
        "description": "TuxD agents will connect directly to this Home Assistant instance instead of through MQTT. This is a *pairing* key, not a permanent credential - paste it into a new agent's tuxd.conf under home-assistant.pairing_key (with tuxd.connection_mode set to \"direct\") to introduce it, then open this integration's Configure button to approve it and issue it its own permanent key. It's reusable - the same key introduces every new device you add from now on, and you can see it again any time on the Configure screen:\n\n`{pairing_key}`\n\nIf you want to invalidate it, use Reconfigure to generate a new one. Devices you've already approved are unaffected by that."
      },
      "reconfigure": {
        "title": "Rotate TuxD pairing key",
        "description": "This replaces the pairing key used to introduce brand-new devices. Already-approved devices keep their own dedicated keys and are not affected - this only stops anyone still holding the old pairing key from introducing a new device:\n\n`{pairing_key}`"
      }
    },
    "abort": {
      "single_instance_allowed": "TuxD is already set up - only one instance is needed, since every agent shares the same connection endpoint.",
      "reconfigure_successful": "Pairing key rotated."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Manage TuxD devices",
        "description": "Approve mints a brand new key for a device - paste it into that device's own tuxd.conf. Trust uses the key a device already presented as-is, so it doesn't need to change anything (only offered for a device that presented its own value, not the shared pairing key below). Reject dismisses a pending device. Rotate issues an already-approved device a fresh key (e.g. one that may have leaked); Revoke pulls its key without deleting it from Home Assistant - it can only reconnect after being re-approved. Or set a specific key yourself below, for either a pending or an approved device. To remove a device entirely, delete it from its own device page instead.\n\nPairing key, for introducing a new device:\n\n`{pairing_key}`",
        "data": {
          "approve": "Approve (new key)",
          "trust": "Trust (use its own key)",
          "reject": "Reject",
          "rotate": "Rotate key",
          "revoke": "Revoke",
          "manual_key_device": "Set a specific key for...",
          "manual_key_value": "...to this key"
        }
      },
      "no_pending": {
        "title": "TuxD devices",
        "description": "No devices are pending approval or approved yet.\n\nPairing key, for introducing a new device:\n\n`{pairing_key}`"
      },
      "issued": {
        "title": "New device keys",
        "description": "Paste each key into that device's own tuxd.conf under home-assistant.api_key. Shown only once:\n\n{keys}"
      }
    },
    "error": {
      "key_required": "Pick a device AND enter a key - both are needed to set one manually.",
      "key_collision": "That key is already used by a different device - each device needs its own unique key."
    }
  },
  "issues": {
    "pending_devices": {
      "title": "TuxD device(s) awaiting approval",
      "description": "{count} TuxD device(s) presented the pairing key and are waiting for you to confirm they're legitimate before they get their own permanent key and can report data or receive commands. Click Fix to review them.",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "Approve TuxD devices",
            "description": "These devices presented the pairing key and are waiting for you to confirm they're legitimate before they get their own permanent key and can report data or receive commands. Reject anything you don't recognize.",
            "data": {
              "approve": "Approve",
              "reject": "Reject"
            }
          },
          "issued": {
            "title": "New device keys",
            "description": "Paste each key into that device's own tuxd.conf under home-assistant.api_key. Shown only once:\n\n{keys}"
          }
        }
      }
    }
  }
}
